Haven't made the pushbutton useful yet
Use the variables clk, dt, sw to specifiy which GPIO ports will be assigned to the pinout. 
You can remove all the pygame stuff and the 'number' variable; theyr'e just for testing
replace 'number += 1' and 'number -= 1' with whatever useful function you want
